const express = require('express');
const router = express.Router();

//Controllers
const userController = require('controller/user');
const User = require('controller/user');

//Routes
//router.get('/home', WebView.index);

module.exports = router;
